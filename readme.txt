• only_terminal.py → Programa bruto com interface direto no terminal

• TKinter_APS.py   → Programa com a interface gráfica feita pelo tkinter, feito para aprimorar os conhecimentos do grupo (Extra)

• textos.py        → Arquivo de texto criado externamente para limpar o código

• coordenadas.py   → Arquivos com diversas listas criadas por nós, cada uma com
algumas informações de latitude e longitude entre outras, usada pelo código principal
como uma espécie de "Banco de dados".

• Imagens          → As imagens usadas no projeto